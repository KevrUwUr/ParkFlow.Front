import { HomeView } from "@/components/Home/HomeView";
import { AppLayout } from "@/components/layout/Layout";

const Home = () => {
  return (
    <AppLayout>
      <HomeView />
    </AppLayout>
  );
};

export default Home;
